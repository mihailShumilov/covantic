export function Footer() {
  return (
    <footer
      style={{
        padding: 'var(--space-lg) var(--space-xl)',
        borderTop: '1px solid var(--color-border)',
        textAlign: 'center',
        color: 'var(--color-text-muted)',
        fontSize: '0.8125rem',
      }}
    >
      AgentGuard &mdash; AI Agent Insurance Protocol on Solana
    </footer>
  );
}
