{
  "name": "covantic",
  "version": "0.1.0",
  "private": true,
  "description": "AI Agent Insurance Protocol on Solana",
  "author": "Michael <info@trade-assistance.com>",
  "license": "MIT",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:anchor": "cd packages/anchor && anchor test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\"",
    "db:migrate": "pnpm --filter api run db:migrate",
    "db:seed": "pnpm --filter api run db:seed",
    "docker:up": "docker compose -f docker/docker-compose.yml up -d",
    "docker:down": "docker compose -f docker/docker-compose.yml down",
    "docker:logs": "docker compose -f docker/docker-compose.yml logs -f",
    "setup": "bash scripts/setup-local.sh",
    "init:devnet": "bash scripts/init-devnet.sh",
    "fund:phantom": "pnpm --filter api exec tsx scripts/mint-mock-usdc.ts",
    "webhook:sync": "pnpm --filter api exec tsx scripts/sync-helius-webhook.ts",
    "agent:create": "pnpm --filter api exec tsx scripts/agent-wallet.ts create",
    "agent:fund": "pnpm --filter api exec tsx scripts/agent-wallet.ts fund",
    "agent:trigger": "pnpm --filter api exec tsx scripts/agent-wallet.ts trigger",
    "agent:help": "pnpm --filter api exec tsx scripts/agent-wallet.ts help",
    "stake:vault": "pnpm --filter api exec tsx scripts/stake-vault.ts",
    "fleet:bootstrap": "pnpm --filter api exec tsx scripts/fleet-bootstrap.ts",
    "fleet:start": "pnpm --filter api exec tsx scripts/fleet-start.ts",
    "fleet:status": "pnpm --filter api exec tsx scripts/fleet-status.ts",
    "deploy:devnet": "bash scripts/deploy-devnet.sh",
    "demo": "tsx scripts/run-demo.ts",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7",
    "@typescript-eslint/parser": "^7",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1",
    "turbo": "^2.9.5"
  }
}
